# ============================================
# AiCMR Environment Configuration
# ============================================
# Copy this file to .env and update values
# Platform-specific overrides documented below
# ============================================

# ============================================
# Database Configuration
# ============================================
DB_NAME=aicmr
DB_USER=aicmr_user
DB_PASSWORD=your_secure_password_change_this
MYSQL_ROOT_PASSWORD=your_root_password_change_this

# IMPORTANT: Generate secure passwords for production!
# PowerShell command (Windows):
#   -join ((48..57) + (65..90) + (97..122) + 33 + 95 | Get-Random -Count 32 | % {[char]$_})
# Python command (All platforms):
#   python -c "import secrets; import string; import random; chars = string.ascii_letters + string.digits + string.punctuation; print(''.join(random.choice(chars) for _ in range(32)))"
# Recommended: Minimum 24 characters, mixed case, numbers, special chars

# ============================================
# Backend Configuration (FastAPI)
# ============================================
# IMPORTANT: Generate a secure SECRET_KEY for production!
# PowerShell command (Windows):
#   python -c "import secrets; print('SECRET_KEY=' + secrets.token_urlsafe(32))"
# Python command (All platforms):
#   python -c "import secrets; print('SECRET_KEY=' + secrets.token_urlsafe(32))"
# Requirements: Minimum 32 characters, random and unpredictable
SECRET_KEY=change-me-to-a-secure-random-key-at-least-32-characters-long
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
DEBUG=false

# IMPORTANT: Generate a secure INSTALL_SECRET for production!
# This secret is used for initial system installation and setup
# PowerShell command (Windows):
#   python -c "import secrets; print('INSTALL_SECRET=' + secrets.token_urlsafe(32))"
# Python command (All platforms):
#   python -c "import secrets; print('INSTALL_SECRET=' + secrets.token_urlsafe(32))"
INSTALL_SECRET=your_random_install_secret_change_this_in_production

# ============================================
# Platform-Specific Configuration
# ============================================

# Linux/Mac (Docker container networking)
# DATABASE_URL=mysql+aiomysql://${DB_USER}:${DB_PASSWORD}@mysql:3306/${DB_NAME}
# CHOKIDAR_USEPOLLING=false

# Windows (Docker Desktop with host.docker.internal)
# DATABASE_URL=mysql+aiomysql://${DB_USER}:${DB_PASSWORD}@host.docker.internal:3306/${DB_NAME}
# CHOKIDAR_USEPOLLING=true (required for file watching on Windows)

# ============================================
# Development (Choose ONE based on your platform)
# ============================================

# OPTION A: Linux/Mac Development
DATABASE_URL=mysql+aiomysql://${DB_USER}:${DB_PASSWORD}@mysql:3306/${DB_NAME}
NEXT_PUBLIC_API_URL=http://aicmr.local/backend
NEXT_PUBLIC_APP_URL=http://aicmr.local
# CHOKIDAR_USEPOLLING=false  # Not needed on Linux/Mac

# OPTION B: Windows Development (UNCOMMENT these for Windows)
# DATABASE_URL=mysql+aiomysql://${DB_USER}:${DB_PASSWORD}@host.docker.internal:3306/${DB_NAME}
# NEXT_PUBLIC_API_URL=http://localhost:8000
# NEXT_PUBLIC_APP_URL=http://localhost:3000
# CHOKIDAR_USEPOLLING=true  # Required for file watching on Windows

# ============================================
# Logging Configuration
# ============================================
LOG_LEVEL=INFO

# Development: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Production: INFO, WARNING, ERROR, CRITICAL (avoid DEBUG in production)

# ============================================
# CORS Configuration
# ============================================
# Comma-separated list of allowed origins for CORS
# Linux/Mac: http://aicmr.local,https://aicmr.local
# Windows: http://localhost:3000,http://localhost:8000,http://aicmr.local (if using domain-based routing)
ALLOWED_ORIGINS=http://localhost:3000,http://aicmr.local,https://aicmr.local,http://127.0.0.1:3000

# ============================================
# Frontend Configuration (Next.js)
# ============================================
# These are already set above, but documented here for clarity
# NEXT_PUBLIC_API_URL=http://aicmr.local/backend
# NEXT_PUBLIC_APP_URL=http://aicmr.local

# ============================================
# Domain Configuration (Production)
# ============================================
# Thay đổi domain ở đây cho production
API_URL=https://api.yourdomain.com
APP_URL=https://yourdomain.com
WS_URL=wss://api.yourdomain.com

# ============================================
# SSL Configuration
# ============================================
# Only needed for HTTPS setup
SSL_CERT_PATH=/etc/nginx/ssl/cert.pem
SSL_KEY_PATH=/etc/nginx/ssl/key.pem

# ============================================
# CLI Configuration
# ============================================
# Tên CLI - có thể tùy biến
# Ví dụ: cli, commander, tool, ai
CLI_NAME=cli

# Mô tả CLI - hiển thị trong header
CLI_DESCRIPTION=AiCMR CLI - Quản lý hệ thống AiCMR

# Tên project - hiển thị trong help
PROJECT_NAME=AiCMR
